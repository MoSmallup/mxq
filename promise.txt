异步任务加载的过程有两步
1、主任务完成
2、遍历任务队列，将队列中的任务逐一执行


JavaScript为什么引入异步？
1、解决js阻塞问题
2、同步代码会阻塞后面代码，异步不会



首先js是单线程，1.有了多线程可以解决js的阻塞问题  
2.同步代码会阻塞后面的代码，异步不会阻塞程序的运行


回调地狱的缺点
1.不便于阅读
2.不便于日常处理


Promise的resolve方法比较特殊，相对于then和catch方法不同
是属于Promise函数对象，并不属于实例化对象
